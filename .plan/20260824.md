# 2026-08-24 增量开发计划

## 今日任务
**Phase 0 · Task 1: 写 `md_to_json.py` / `csv_to_json.py` 解析脚本**

## 做什么
为后续词表/语法/场景/翻译规则数据入库铺路。先写两个轻量解析器,定义词条/语法点/场景的统一 JSON schema,供 Phase 0 后 6 项任务复用。

## 涉及文件
- 新建 `scripts/md_to_json.py` —— 解析 `语法/*.md` 章节化内容(按 ## / ### 切分)→ JSON
- 新建 `scripts/csv_to_json.py` —— 解析 `词表/*.csv`(列:word/pos/pronunciation/meaning_cn/difficulty/tags)→ JSON
- 新建 `data/schema/vocab.schema.json` —— 词条统一 schema(对应项目开发计划 §4.3 vocab 表字段)
- 新建 `data/schema/scene.schema.json` —— 场景 schema(预留,场景库任务时使用)

## 验收
1. `python3 scripts/md_to_json.py --input <测试md> --output /tmp/out.json` 跑通,产物符合 schema
2. `python3 scripts/csv_to_json.py --input <测试csv> --output /tmp/out.json` 跑通
3. 两个脚本均支持 `--input` / `--output` / `--pretty` 三个 CLI 参数
4. 不引入第三方依赖(纯 stdlib: `argparse` / `json` / `csv` / `re`)
5. README 增补 `scripts/` 使用说明小节

## 不做什么
- 不动 `项目开发计划.md` 主文件(T1 大计划)
- 不开始建词条/词根/场景数据本身(那是 Phase 0 Task 2-4,后几天再做)
- 不动 React/FastAPI 骨架(Phase 1 的事)
